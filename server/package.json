{
  "name": "flexio-server",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Fejlesztői eszközök: magyar ételkatalógus összeállítása. A DB seed és OFF import a C# API-n fut.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build:catalog": "node scripts/build_catalog.mjs"
  }
}
